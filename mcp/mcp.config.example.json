{
  "_comment": "Example Claude Code MCP registration for the vss server (A1 scaffold). Merge into your project .mcp.json or run `claude mcp add`. Adjust the python path and env. The tools are stubs until the live integration is written — see vss-codex/mcp/README.md.",
  "mcpServers": {
    "vss": {
      "command": "python",
      "args": ["vss-codex/mcp/server/server.py"],
      "env": {
        "VSS_SSH_HOST": "user@your-vs-host",
        "VSS_CONTAINER": "your-vs-container"
      }
    }
  }
}
